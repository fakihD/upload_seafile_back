(function() {
  var config, mod;

  mod = {};

  mod.api = require('./api');

  mod.auth = require('./auth');

  mod.library = require('./library');

  mod.directory = require('./directory');

  config = require('./config');

  module.exports = function(url) {
    config.url = url;
    return mod;
  };

  config.url = 'https://files.riksolo.com';

  mod.auth.getToken('test@example.com', 'testing1', function(token) {});

}).call(this);
