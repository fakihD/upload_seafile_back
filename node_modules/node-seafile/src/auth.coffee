config = require './config'
api = require './api'

this.getToken = (username, password, cb)->
	api.post 'auth-token', {username: username, password: password}, (err, res)->
		throw err if err
		token = JSON.parse(res).token
		cb token

module.exports = this
